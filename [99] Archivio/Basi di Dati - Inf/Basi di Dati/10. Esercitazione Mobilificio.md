1. Quali sono i componenti con costo inferiore a 100,00 euro?

```sql
SELECT com_descrizione, com_costo
FROM Componenti
WHERE com_costo < 100.00
```

1. Visualizzare indirizzo, città e telefono del negozio ‘MobilMarket’

```sql
SELECT neg_indirizzo, neg_citta, neg_telefono
FROM Negozi 
WHERE neg_nome = "MobilMarket"
```

1. Selezionare i negozi con sede a Firenze

```sql
SELECT neg_nome
FROM Negozi
WHERE neg_citta = "Firenze"
```

1. Selezionare gli articoli il cui costo lordo (con IVA) sia inferiore a 500 euro

```sql
SELECT art_descrizione, art_prezzo
FROM Articoli
WHERE (art_prezzo+art_IVA)<500.00
```

1. Selezionare gli articoli il cui prezzo lordo sia inferiore a 500 euro, e le cui spese di trasporto siano superiori a 20 euro

```sql
SELECT art_descrizione 
FROM Articoli
WHERE (art_prezzo+art_IVA)<500.00 AND art_spese_trasporto > 20
```

1. Selezionare i negozi la cui sede sia a Roma o a Firenze

```sql
SELECT neg_nome, neg_indirizzo
FROM Negozi
WHERE neg_citta = "Roma" OR neg_citta = "Firenze"
```

1. Selezionare i componenti il cui costo sia compreso tra 50 e 100 euro

```sql
SELECT com_descrizione
FROM Componenti
WHERE com_costo BETWEEN 50 AND 100
```

1. Selezionare gli ordini in cui la quantità di articoli richiesti sia compresa tra 15 e 30

```sql
SELECT ord_cod, ord_qta
FROM Ordart
WHERE ord_qta BETWEEN 15 AND 30
```

1. Selezionare tutti i negozi la cui sede sia a Firenze o Roma usando l’operatore IN

```sql
SELECT neg_nome, neg_citta
FROM Negozi
WHERE neg_citta IN ("Roma", "Firenze")
```

1. Selezionare i componenti con costo uguale a 100 euro o a 120 euro

```sql
SELECT com_descrizione, com_prezzo
FROM Componenti
WHERE com_prezzo = 100 OR com_prezzo = 120
```

1. Selezionare tutti i componenti il cui codice termina per 10

```sql
SELECT com_descrizione
FROM Componenti
WHERE com_cod LIKE "%10"
```

1. Selezionare tutti i negozi il cui nome inizia per M

```sql
SELECT neg_nome
FROM Negozi
WHERE neg_nome LIKE "M%"
```

1. Selezionare tutti i componenti la cui descrizione contiene la parola ‘Tavolo’

```sql
SELECT com_descrizione
FROM Componenti
WHERE com_descrizione LIKE "%Tavolo%"
```

1. Quali sono i negozi di cui non si conosce il numero di telefono?

```sql
SELECT neg_nome
FROM Negozi
WHERE neg_telefono IS NULL
```

1. Quali sono i componenti di cui non si conosce il laboratorio?

```sql
SELECT com_descrizione
FROM Componenti
WHERE lab_cod IS NULL
```

1. Visualizzare la tabella dei componenti con il prezzo aumentato del 10%

```sql
SELECT com_descrizione, com_prezzo, com_prezzo*1.1 PrezzoAumentato
FROM Componenti
```

1. Calcolare il costo lordo, ovvero comprensivo di IVA del tavolo tondo (codice T100). Rinominare la corrispondente colonna con “Prezzo lordo”

```sql
SELECT art_descrizione, art_prezzo+art_IVA "Prezzo lordo" 
FROM Articoli
WHERE art_cod = "T100"
```

1. Ordinare i negozi di Roma in base ai nomi

```sql
SELECT neg_nome, neg_indirizzo
FROM Negozi
WHERE neg_citta = "Roma" 
ORDER BY neg_nome
```

1. Ordinare i componenti per costi decrescenti

```sql
SELECT com_descrizione
FROM Componenti
ORDER BY com_costo DESC
```

1. Ordinare i componenti per costi decrescenti e, a parità di costo, per codici crescenti

```sql
SELECT com_descrizione, com_costo
FROM Componenti
ORDER BY com_costo DESC, com_cod ASC
```

1. Calcolare il numero di codici presenti nella tabella Articoli; spiegare perché in questo caso non ha senso applicare le specifiche ALL e DISTINCT

```sql
SELECT COUNT(com_cod)
FROM Componenti
```

1. Calcolare il numero totale delle categorie, basandosi sulla tabella categorie.

```sql
SELECT COUNT(cat_cod)
FROM Categorie
```

1. Calcolare il numero di categorie presenti nella tabella Articoli

```sql
SELECT COUNT(DISTINCT cat_cod)
FROM Articoli
```

1. Calcolare il numero di categorie presenti nella tabella Articoli, per cui il prezzo dell’articolo corrispondente sia inferiore a 400 euro.

```sql
SELECT COUNT(DISTINCT cat_cod)
FROM Articoli
WHERE art_prezzo < 400
```

1. Quali sono la massima e la minima quantità di articoli richiesti in un ordine?

```sql
SELECT MIN(ordart_qta), MAX(ordart_qta)
FROM Ordart
```

1. Calcolare a quanto ammonterebbe il prezzo massimo della tabella articoli se vi venisse apportato un aumento del 12%

```sql
SELECT MAX(art_prezzo*1.12) "Prezzo Maggiorato"
FROM Articoli
```

1. Determinare qual è il costo minimo di un articolo, considerato il suo prezzo, l’iva e le spese di trasporto

```sql
SELECT MIN(art_prezzo+art_IVA+art_spese_trasporto)
FROM Articoli
```

1. Sommare i prezzi di tutti i componenti

```sql
SELECT SUM(com_costo)
FROM Componenti
```

1. Sommare i prezzi distinti di tutti i componenti

```sql
SELECT SUM(DISTICT com_costo)
FROM Componenti
```

1. Sommare i prezzi di tutti i componenti prodotti nel laboratorio dal codice ‘0050’

```sql
SELECT SUM(com_costo) 
FROM Componenti
WHERE lab_cod = "0050"
```

1. Calcolare la somma dei prezzi dei due armadi presenti nella tabella Articoli

```sql
SELECT SUM(art_prezzo)
FROM Articoli
WHERE art_cod = "M_40" OR art_cod = "M_50"
```

1. Calcolare il numero totale di articoli ordinati

```sql
SELECT SUM(ordart_qta)
FROM Ordart
```

1. Calcolare la media delle spese di trasporto degli articoli

```sql
SELECT AVG(art_spese_trasporto)
FROM Articoli
```

1. Calcolare la media dei prezzi dei componenti

```sql
SELECT AVG(com_costo)
FROM Componenti
```

1. Calcolare la media dei prezzi lordi degli articoli, inclusivi di spese di trasporto (per gli articoli per cui prezzo, IVA e Spese trasporto sono definite)

```sql
SELECT AVG(art_prezzo+art_IVA+art_spese_trasporto)
FROM Articoli
WHERE art_prezzo IS NOT NULL AND art_IVA IS NOT NULL AND art_spese_trasporto IS NOT NULL
```

1. Calcolare il prezzo più alto degli articoli per ciascuna categoria.

```sql
SELECT cat_cod, MAX(art_prezzo)
FROM Articoli
GROUP BY cat_cod
```

1. Calcolare il numero di articoli appartenenti a ciascuna categoria

```sql
SELECT cat_cod, COUNT(*)
FROM Articoli
GROUP BY cat_cod
```

1. Calcolare il numero di componenti di ciascun articolo.

```sql
SELECT a.art_descrizione, COUNT(compart_qta)
FROM Articoli a, Compart c
WHERE a.art_cod = c.art_cod
GROUP BY a.art_descrizione
```

1. Calcolare quanti articoli sono richiesti in ciascun ordine

```sql
SELECT ord_cod, SUM(ordart_qta)
FROM Ordart
GROUP BY ord_cod
```

1. Visualizzare per ciascun articolo la sua descrizione e i codici dei suoi componenti

```sql
SELECT a.art_descrizione, ca.com_cod
FROM Articoli a, Compart ca
WHERE a.art_cod = ca.art_cod
```

1. Visualizzare per ciascun ordine la città del negozio da cui l’ordine è effettuato.

```sql
SELECT o.ord_cod, n.citta
FROM Ordini o, Negozi n
WHERE o.neg_cod = n.neg_cod
```

1. Trovare i codici dei componenti dell’articolo Tavolo Quadrato

```sql
SELECT a.art_descrizione, c.com_cod
FROM Articoli a, Compart c
WHERE a.art_descrizione = "Tavolo quadrato" AND a.art_cod = c.art_cod
```

1. Trovare i codici degli ordini relativi a negozi di Trento

```sql
SELECT o.ord_cod, n.neg_come 
FROM Ordini o, Negozi n
WHERE o.neg_cod = n.neg_cod AND n.neg_citta = "Trento"
```

1. Visualizzare per ciascun ordine l’indirizzo del negozio da cui l’ordine è effettuato e la descrizione degli articoli ordinati.

```sql
SELECT n.neg_indirizzo, a.art_descrizione
FROM Articoli a, Ordart oa, Ordini o, Negozi n
WHERE a.art_cod = oa.art_cod AND oa.ord_cod = o.ord_cod AND o.neg_cod = n.neg_cod
```

1. Trovare la data dell’ordine in cui è stato richiesto l’articolo Tavolo Quadrato

```sql
SELECT o.ord_data
FROM Ordini o, Ordart o1, Articoli a
WHERE o.ord_cod = o1.ord_cod AND o1.art_cod = a.art_cod AND a.art_descrizione = "Tavolo quadrato"
```

1. Trovare la descrizione degli articoli nella cui composizione è presente il componente Anta 100 cm

```sql
SELECT a.art_descrizione
FROM Articoli a, Compart c, Componenti c1
WHERE a.art_cod = c.art_cod AND c.com_cod = c1.com_cod AND c1.com_descrizione = "Anta 100 cm"
```

1. Trovare le categorie degli articoli nella cui composizione è presente il componente dal codice 0010

```sql
SELECT c.cat_descrizione
FROM Categorie c, Articoli a, Compart c1
WHERE c.cat_cod = a.cat_cod AND a.art_cod = c1.art_cod AND c1.com_cod = "0010"
```

1. Trovare le categorie degli articoli nella cui composizione è presente il componente Ripiano 60cm

```sql
SELECT c.cat_descrizione
FROM Categorie c, Articoli a, Compart c1, Componenti c2
WHERE c.cat_cod = a.cat_cod AND a.art_cod = c1.art_cod AND c1.com_cod = c2.com_cod AND c2.com_descrizione = "Ripiano 60 cm" 
```

1. Quali negozi hanno sede nella stessa città di MobilMarket?

```sql
SELECT neg_nome
FROM Negozi 
WHERE neg_citta = (SELECT neg_citta FROM Negozi WHERE neg_nome = "MobilMarket")
```

1. Quali articoli hanno lo stesso prezzo?

```sql
SELECT art_descrizione, art_prezzo 
FROM Articoli 
WHERE art_prezzo IN (SELECT art_prezzo FROM Articoli GROUP BY art_prezzo HAVING COUNT(*) > 1)
ORDER BY art_prezzo
```

1. Trovare tutti gli ordini del negozio Compolegno

```sql
SELECT o.ord_cod
FROM Negozi n, Ordini o
WHERE n.neg_cod = o.neg_cod AND n.neg_cod = "Campolegno"
```

1. Selezionare i nomi dei componenti meno utilizzati nelle composizioni, ovvero che compaiono meno della media nella composizione dei vari articoli.

```sql
SELECT com_descrizione
FROM Componenti c, Compart c1 
WHERE c.com_cod = c1.com_cod AND c1.compart_qta < (SELECT AVG(compart_qta) FROM Compart)
```

1. Selezionare i componenti il cui costo è superiore al prezzo dell’articolo T400.

```sql
SELECT c.com_descrizione
FROM Componenti c
WHERE c.com_costo > (SELECT art_prezzo FROM Articoli WHERE art_cod = "T400")
```

1. Selezionare gli articoli che costano meno del componente col prezzo più alto

```sql
SELECT art_descrizione
FROM Articoli
WHERE art_prezzo < (SELECT MAX(com_costo) FROM Componenti)
```

1. Selezionare gli articoli con prezzo inferiore al costo di almeno un componente, senza utilizzare la funzione max

```sql
SELECT art_descrizione
FROM Articoli
WHERE art_prezzo < ANY (SELECT com_costo FROM Componenti)
```

1. Selezionare gli articoli con prezzo inferiore al costo di tutti i componenti, senza usare la funzione min.

```sql
SELECT art_descrizione
FROM Articoli
WHERE art_prezzo < ALL(SELECT com_costo FROM Componenti)
```

1. Trovare i codici degli articoli in cui c’è almeno un componente non prodotto in laboratori

```sql
SELECT art_descrizione
FROM Articoli
WHERE EXISTS (SELECT * 
							FROM Componenti c, Compart c2 
							WHERE art_cod = c2.art_cod AND c2.com_cod = c.com_cod AND c.lab_cod IS NULL)

```

1. Elencare tutte le città in cui sono presenti sia laboratori che negozi

```sql
SELECT DISTINCT neg_citta
FROM Negozi
WHERE neg_citta IN (SELECT lab_citta FROM Laboratori)
```

1. Trovare tutti i componenti prodotti nei laboratori usando la clausola Exists

```sql
SELECT c.com_descrizione
FROM Componenti c
WHERE EXISTS (SELECT * FROM Laboratori l WHERE c.lab_cod = l.lab_cod)
```

1. Trovare tutti gli articoli per cui non vi sono ordini usando la clausola Exists

```sql
SELECT art_descrizione
FROM Articoli a
WHERE NOT EXISTS (SELECT * FROM Ordart o WHERE a.art_cod = o.art_cod)
```

1. Cercare per il componente ‘Anta 100cm’ la differenza rispetto al costo medio dei componenti

```sql
SELECT com_costo - (SELECT AVG(com_costo) FROM Componenti)
FROM Componenti 
WHERE com_descrizione = "Anta 100 cm"
```

1. Determinare quanto viene a costare ciascun tipo di componente della composizione di ogni articolo, tenendo in considerazione la quantità utilizzata.

```sql
SELECT a.art_descrizione, c.com_cod, c.compart_qta*c1.com_costo
FROM Articoli a, Compart c, Componenti c1
WHERE a.art_cod = c.art_cod AND c.com_cod = c1.com_cod
```

1. Trovare il nome del negozio che ha ordinato il quantitativo massimo di articoli di uno stesso tipo

```sql
SELECT n.neg_nome
FROM Negozi n, Ordini o, Ordart o1
WHERE n.neg_cod = o.neg_cod AND o.ord_com = o1.ord_com AND o1.ordart_qta = (SELECT MAX(ordart_qta) FROM Ordart)
```

1. Creare una vista che mostri i codici dell’ordine e del negozio per gli ordini effettuati dopo il 31/01/2004

```sql
CREATE VIEW OrdiniDopo2004 AS
SELECT n.meg_cod, o.ord_cod
FROM Negozi n, Ordini o
WHERE n.neg_cod = o.neg_cod AND o.ord_data > '31/01/2004'
```

1. Creare una vista che calcoli i codici dei componenti utilizzati nella composizione dell’articolo L100

```sql
CREATE VIEW CodiciComponentiL100 AS
SELECT c.com_cod
FROM Articoli a, Compart c
WHERE a.art_cod = c.art_cod AND a.art_cod = "L100"
```

1. Creare una vista che mostri le descrizioni dei componenti utilizzati nella composizione dell’articolo L100

```sql
CREATE VIEW DescComponentiL100 AS
SELECT c1.com_descrizione
FROM Compart c, Componenti c1
WHERE c.com_cod = c1.com_cod AND c.art_cod = "L100"
```

1. Creare una vista che mostri le descrizioni dei componenti utilizzati nella composizione degli articoli della categoria L10

```sql
CREATE VIEW DescComponentiCategoriaL10 AS
SELECT c1.com__descrizione
FROM Articoli a, Compart c, Componenti c1
WHERE a.art_cod = c.art_cod AND c.com_cod = c1.com_cod AND a.cat_cod = "L10"
```

1. Creare una vista che mostri la massima quantità richiesta in un ordine per ciascun articolo ordinato

```sql
CREATE VIEW MassimaQuantitaPerArticolo AS
SELECT a.art_cod, MAX(o.ordart_qta)
FROM Articoli a, Ordart o
WHERE a.ord_cod = o.ord_cod
GROUP BY a.art_cod
```

1. Eliminare la vista creata all’esercizio precedente

```sql
DROP VIEW MassimaQuantitaPerArticolo 
```

1. Trovare quali sono gli articoli che nella loro composizione hanno il maggior numero di viti.

```sql
SELECT a.art_descrizione, c.compart_qta 
FROM Articoli a, Compart c, Componenti c3
WHERE a.art_cod = c.art_cod AND c.com_cod = c3.com_cod AND c3.com_descrizione LIKE "%viti%" 
												AND  c.compart_qta = (SELECT MAX(c1.compart_qta)
												FROM Compart c1, Componenti c2
												WHERE c1.com_cod = c2.com_cod AND c2.com_descrizione LIKE "%viti%"	)
```

1. Determinare qual è la categoria i cui articoli sono in media più cari

```sql
SELECT c.cat_descrizione 
FROM Categorie c, Articoli a 
WHERE c.cat_cod = a.cat_cod 
GROUP BY a.cat_cod 
HAVING AVG(a.art_prezzo) > (SELECT AVG(a.art_prezzo )
													  FROM Articoli a)
```

1. Determinare i negozi che hanno ordinato il maggior numero di articoli della categoria Libreria

```sql
SELECT n.neg_nome 
FROM Negozi n, Ordini o , Ordart o2 
WHERE n.neg_cod = o.neg_cod AND o.ord_cod = o2.ord_cod AND o2.ordart_qta = (SELECT MAX(o3.ordart_qta )
																			FROM Ordart o3 , Articoli a , Categorie c 
																			WHERE o3.art_cod = a.art_cod AND a.cat_cod = c.cat_cod AND c.cat_descrizione = "Libreria")
```

1. Quali laboratori producono in media i prodotti meno cari

```sql
SELECT l.lab_cod 
FROM Laboratori l , Componenti c 
WHERE l.lab_cod = c.lab_cod 
GROUP BY l.lab_cod 
HAVING AVG(c.com_costo ) < (SELECT AVG(c2.com_costo) FROM Componenti c2 )
```

1. Determinare quali sono le categorie i cui articoli costano tutti più di 300 euro

```sql
SELECT c.cat_descrizione 
FROM Categorie c 
WHERE 300 < ALL(SELECT a.art_prezzo  FROM Articoli a WHERE a.cat_cod = c.cat_cod  );
```

1. Scrivere quali sono le categorie di articoli tali che nessuno di essi utilizza come componente il ripiano 60 cm

```sql
SELECT c.cat_descrizione 
FROM Categorie c 
WHERE c.cat_cod NOT IN (SELECT a.cat_cod 
						FROM Articoli a , Compart c , Componenti c2 
						WHERE a.art_cod = c.art_cod AND c.com_cod = c2.com_cod AND c2.com_descrizione = "Ripiano 60 cm")

```