Problema indecidibile → Un problema per cui non esiste un algoritmo che ne produce la soluzione

### Decidibilità dei problemi

|  | Membership | Emptyness | Finiteness | Equivalence |
| --- | --- | --- | --- | --- |
| Macchina di turing |  |  |  |  |
| Context sensitive |  |  |  |  |
| Context-free | Si | Si | SI | No |
| Automi a stati finiti | Si | Si | Si | Si |

### Membership problem per i DFA

Data una stringa $x$ ed un automa $A$. $A$ riconosce $x?$

Banalmente, seguo il percorso dell’automa con $x$ e vedo se è accettata - $O(|x|)$

### Emptyness problem per i DFA

$L(A)=\emptyset?$

Th.   $L(A)\ne\emptyset\Leftrightarrow$ riconosce $w$ con $|w|<n$ numero di stati

$(\Leftarrow)$ banale

$(\Rightarrow)$ se $L(A)\ne\emptyset$ allora riconosce una parola $w$

Se $|w|<n$ abbiamo dimostrato. Se $|w|>n$, per il pumping lemma esiste un ciclo, e che $w=uv^0x\in L$. Azzeriamo tutti i cicli finché $|uv^0x|<n$

Problema decidibile in $O(n^2)$

### Finiteness problem per i DFA

$L(A)$ è un linguaggio finito?

Th.   $L(A)$ è infinito $\Leftrightarrow$ riconosce $w$ tale che $n<|w|<2n$

$(\Leftarrow)$ sia $w$ con $n<|w|<2n$ accettata. Allora per il pumping lemma $w=xyz,|y|\ge1$ quindi avremo $\{xy^iz\ |\ i\in\mathbb{N}\}\subseteq L$ e quindi $L$ infinito

$(\Rightarrow)$ sia $L(A)$  infinito. Allora $\exists\ w\in L(A)$ tale che $|w|\ge n$. Per il pumping lemma posso fattorizzare in $w=xyz$, con $|xy|<n$ e $|y|\ge1$. E anche $xz\in L(A)$. Se $|xz|<n$, tesi dimostrata, altrimenti reitero su $xz$ finché non lo è.

### Equivalence problem per i DFA

Dati $A_1$ e $A_2$ automi, $L(A_1)=L(A_2)?$

Metodo 1: minimizzo gli automi. Essendo il minimo unico, li confronto e vedo se sono uguali

Metodo 2: Dobbiamo verificare che 

$$
(L(A_1)\cap L(A_2)^c)\cup(L(A_1)^c\cap L(A_2))\ne\emptyset
$$

Quindi il problema è equivalente all’emptyness problem.

### Membership problem per CFG

Parola $x\in L(G)?$

Decidibile generando tutte le derivazioni con $|x|$ foglie

### Emptyness Problem per CFG

$L(G)=\emptyset?$

Decidibile verificando che $S$ produce almeno una stringa di tutti i terminali. Essendo le produzioni finite, $O(n^2)$

### Finiteness problem per CFG

$L(G)$ è finito?

Trasformiamo la grammatica in CNF e creiamo un grafo con:

- un nodo per ogni $A\in V$
- un arco $<A,B>$, e $<A,C>$ per ogni produzione $A\rightarrow BC$

$L(G)$ è finito $\Leftrightarrow$ il grafo non ha cicli

### Equivalence problem per CFG

$L(G_1)=L(G_2)?$

è indecidibile

Altri problemi

- Ambiguità di una grammatica
- Ambiguità di un linguaggio

Entrambi i problemi sono indecidibili